# Maintainer: Colin Teng <tfcolin@88.com>
pkgname=slidegame
pkgver=1.0.0
pkgrel=1
pkgdesc="一个滑块拼图游戏, 采用前后台分离设计模式. 后台负责游戏规则实现和流程控制, 前台负责与用户交互. UI 开发者可以基于后台部分开发不同的用户界面."
arch=('x86_64')
url="https://gitee.com/tfcolin/slide"
license=('GPL-3.0-or-later')
depends=('gtk3' 'ncurses')
source=("$pkgname-$pkgver::https://gitee.com/tfcolin/slide.git")
sha256sums=('88e931765814df4da5dbc7f1397232540302c1da611cd752fb4ba141aa97f570')

build() {
	cd "$pkgname-$pkgver"
	make
}

package() {
	cd "$pkgname-$pkgver"
	PREFIX="$pkgdir/usr/" make install
}
